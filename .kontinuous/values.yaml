# yaml-language-server: $schema=../../kontinuous/docs/values.schema.json

app:
  containerPort: 3000
  probesPath: /healthz
  envFrom:
    - secretRef:
        name: "pg-user"
    - secretRef:
        name: api

pg:
  ~chart: pg

jobs:
  runs:
    build-app:
      with:
        context: api

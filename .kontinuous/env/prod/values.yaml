app:
  host: api-ozensemble.fabrique.social.gouv.fr

cronjobs:
  ~chart: daemon
  imagePackage: app
  entryPoint: ["node", "./src/cronjobs.js"]
  envFrom:
    - secretRef:
        name: "pg-user"
    - secretRef:
        name: api
